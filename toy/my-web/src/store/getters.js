export default {
    fetchedGitRepoData(state) {
        return state.repoData;
    },
    fetchedGitContentData(state) {
        return state.repoContent;
    },
    fetchedPosts(state) {
        return state.posts;
    }
};